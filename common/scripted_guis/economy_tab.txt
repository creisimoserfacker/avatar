
scripted_gui = {

	economy_tab_ui = {
		context_type = player_context
		window_name = "economy_tab_ui_window"
		parent_window_token = construction_tab

		triggers = {
			
		}

		properties = {
			
		}
	}

	add_resource_building_ui = {
		context_type = player_context
		window_name = "add_resource_building_ui_window"
		parent_window_token = construction_tab

		triggers = {
			
		}

		properties = {
			
		}
	}
	add_resource_building_ui = {
		context_type = player_context
		window_name = "add_resource_building_ui_window"
		parent_window_token = construction_tab

		triggers = {
			
		}

		properties = {
			
		}
	}
	
}
